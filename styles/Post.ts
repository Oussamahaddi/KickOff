import { Dimensions } from "react-native"

const BLACK = "black"
const WHITE = "white"
const DIMESION_HEIGHT = Dimensions.get('screen').height;
const DIMESION_WIDTH = Dimensions.get('screen').width;

const FONTWEIGHT = {
  sm : '200',
  md : '500',
  lg : '600',
  xl : '700',
}

export {WHITE, BLACK, DIMESION_HEIGHT, DIMESION_WIDTH, FONTWEIGHT}